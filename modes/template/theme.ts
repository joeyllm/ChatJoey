import type { JoeyTheme } from "../types";

// Every field is optional — leave a field out and Joey's normal default
// applies. Only set what your mode actually needs to change.
const theme: JoeyTheme = {
  // accent: "#4a7a5e",
  // accentDeep: "#35604a",
  // background: "linear-gradient(180deg, #b9d9f0 0%, #ecdec0 100%)",
  // userMessage: "rgba(74, 122, 94, 0.1)",
  // speechBubble: "#fdf3df",
  // sidebarTint: "#ffffff",
};

export default theme;
